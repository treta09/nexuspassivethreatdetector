# Limitations

This repository does not generate traffic, invent benchmark values, decrypt payloads, or claim model performance before evaluation. Supply labeled flow and protocol-metadata datasets, train models under `ml/training/`, and publish measured precision, recall, F1, latency, and throughput separately.