# Frontend boundary

The production Vite frontend currently lives at the repository root so the existing Vercel deployment remains stable. This directory reserves the requested split-layout boundary for a future extraction of `src/`; do not duplicate or maintain a second frontend here.