services:
  detector:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    environment:
      NEXUS_READ_ONLY: "true"
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000